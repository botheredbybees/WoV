# WoV/backend/image-proxy/Dockerfile
# This is a placeholder Dockerfile to allow the service to build.
# You will replace this with your actual image proxy application's Dockerfile later.

FROM alpine:latest

# Set a working directory
WORKDIR /app

# Expose the port that the image proxy will listen on (as defined in docker-compose.yml)
EXPOSE 5001

# Simple command to keep the container running for now.
# Replace this with your actual image proxy application's entrypoint.
CMD ["tail", "-f", "/dev/null"]
